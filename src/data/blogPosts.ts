import { BlogPost } from "@/types/blog";

// Static blog posts are no longer needed as the application now uses the API
// All blog data is fetched from the backend API endpoints
export const blogPosts: BlogPost[] = [];
];
